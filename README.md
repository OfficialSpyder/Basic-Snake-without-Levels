# Basic-Snake-without-Levels

# Install this game in your systems (PC and Laptop Only)
`First you need to install the python latest version in your system. Make sure if you have already installed python then check its latest or not`
`pip install git`
`git clone https://github.com/OfficialSpyder/Basic-Snake-without-Levels`
`python snake.py`
